{
  "permissions": {
    "allow": [
      "Bash(python -c \"from forge.display import build_metrics_table; from forge.cli import main; print\\('OK'\\)\")",
      "Bash(python -c \"from forge.cli import main; from forge.commands import grid, failed_runs; from forge.display import build_metrics_table, shorten_keys; print\\('OK'\\)\")"
    ]
  }
}
